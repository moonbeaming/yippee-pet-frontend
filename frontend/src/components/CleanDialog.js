import { Dialog, Button } from "@mui/material";
import React, { useState } from "react";

const CleanDialog = () => {
  return (
    <div>
      <Dialog open>
      </Dialog>
    </div>
  );
};

export default CleanDialog;
